from django.urls import path
from .views import *

urlpatterns= [
    path('', Countries, name='Countries'),
    # path('', Countries, name='Countries'),
    # path('', Countries, name='Countries'),
    # path('', Countries, name='Countries'),
    # path('', Countries, name='Countries'),
    # path('', Countries, name='Countries'),
]